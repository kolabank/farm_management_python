const crop_selected = document.querySelector('#crop');
crop_selected.addEventListener("change", () => {
    console.log("The crop is", crop_selected.value);
})

function myFunc(vars) {
    return vars
}